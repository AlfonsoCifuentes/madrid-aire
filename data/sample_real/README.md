# Official sample snapshots

Files stored here must come from official sources only.

Recommended workflow:

1. Use `python data/download_air_quality.py ...`.
2. Keep the sidecar metadata JSON committed with the snapshot.
3. Do not rename files into generic mock-like names.
